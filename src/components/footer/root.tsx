import { InnerContainer } from "../innerContainer";

export function Footer() {
  return (
    <footer className="w-full flex justify-center bg-slate-400">
      <InnerContainer>footer componente raiz</InnerContainer>
    </footer>
  );
}
