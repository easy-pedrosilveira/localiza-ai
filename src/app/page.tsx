import { InnerContainer } from "@/components";

export default function Home() {
  return (
    <main className="w-full h-[89vh] grid items-center justify-center bg-red-300">
      <InnerContainer>BUSCA CEP</InnerContainer>
    </main>
  );
}
